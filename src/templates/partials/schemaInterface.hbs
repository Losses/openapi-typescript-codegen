{
    properties: {
{{#if properties}}
    {{#each properties}}
        {{#if isReference}}
        {{{name}}}: ${{{base}}},
        {{else}}
        {{{name}}}: {{>schema}},
        {{/if}}
    {{/each}}
{{/if}}
    },
{{#if isReadOnly}}
    isReadOnly: {{{isReadOnly}}},
{{/if}}
{{#if isRequired}}
    isRequired: {{{isRequired}}},
{{/if}}
{{#if isNullable}}
    isNullable: {{{isNullable}}},
{{/if}}
}
