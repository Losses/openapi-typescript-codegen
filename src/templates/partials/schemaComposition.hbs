{
    type: '{{export}}',
    contains: [{{#each properties}}{{>schema}}{{#unless @last}}, {{/unless}}{{/each}}],
{{#if isReadOnly}}
    isReadOnly: {{{isReadOnly}}},
{{/if}}
{{#if isNullable}}
    nullable: {{{isNullable}}},
{{/if}}
}
