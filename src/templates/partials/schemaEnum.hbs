{
    type: '{{{base}}}',
    enum: [{{#each enum}}{{{value}}}{{#unless @last}}, {{/unless}}{{/each}}],
{{#if isReadOnly}}
    readonly: {{{isReadOnly}}},
{{/if}}
{{#if isRequired}}
    required: {{{isRequired}}},
{{/if}}
{{#if isNullable}}
    nullable: {{{isNullable}}},
{{/if}}
}
