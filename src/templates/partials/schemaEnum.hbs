{
    type: '{{{base}}}',
    enum: [{{#each enum}}{{{value}}}{{#unless @last}}, {{/unless}}{{/each}}],
{{#if isReadOnly}}
    isReadOnly: {{{isReadOnly}}},
{{/if}}
{{#if isRequired}}
    isRequired: {{{isRequired}}},
{{/if}}
{{#if isNullable}}
    isNullable: {{{isNullable}}},
{{/if}}
}
