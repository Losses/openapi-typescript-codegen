{
    type: 'dictionary',
{{#if link}}
    contains: {{>schema link}},
{{else}}
{{#if isReference}}
    contains: ${{{base}}},
{{else}}
    contains: {
        type: '{{{base}}}',
    },
{{/if}}
{{/if}}
{{#if isReadOnly}}
    isReadOnly: {{{isReadOnly}}},
{{/if}}
{{#if isRequired}}
    isRequired: {{{isRequired}}},
{{/if}}
{{#if isNullable}}
    isNullable: {{{isNullable}}},
{{/if}}
}
